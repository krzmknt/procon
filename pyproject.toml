[project]
name = "procon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.1",
]

[dependency-groups]
dev = [
    "autopep8>=2.3.1",
    "online-judge-tools>=11.5.1",
    "pytest>=8.3.4",
]
